import express from "express";

export const roleList: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const roleGet: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const roleCreate: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const roleUpdate: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const roleDelete: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};
