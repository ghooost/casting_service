import express from "express";

export const slotList: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const slotGet: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const slotCreate: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const slotUpdate: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};

export const slotDelete: express.RequestHandler = (request, response) => {
  response.sendStatus(200);
};
